# CHANGE LOG

## 0.0.1

- Initial version.
- Add `IStorage` interface for general key-value storage
- Add `ISecureStorage` interface for encrypted storage
- Support string, int, bool data types
- Integrate with `domain` package's `Result<Failure, T>` pattern
