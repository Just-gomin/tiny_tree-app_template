name: utils
description: Pure Dart utility extensions and helpers for TinyTree
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  intl: ^0.20.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
