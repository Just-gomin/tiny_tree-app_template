# 가위바위보 게임 MVP

## 개요

노년층이 즐길 수 있는 큰 UI의 가위바위보 게임입니다.

- **앱 이름**: 가위바위보
- **목표 사용자**: 노년층 (60세 이상)
- **플랫폼**: Flutter Web

## 핵심 기능 (3개)

| 기능 | 설명 |
|:---:|:---:|
| 1. 게임 플레이 | 가위/바위/보 버튼을 눌러 컴퓨터와 대결 |
| 2. 결과 표시 | 승/패/무승부 결과를 크고 명확하게 표시 |
| 3. 전적 기록 | 현재 세션의 승/패/무승부 횟수 표시 |

## 화면 구성 (단일 화면)

### 메인 게임 화면

```
┌─────────────────────────────────────┐
│           가위바위보 게임            │  ← 큰 제목
├─────────────────────────────────────┤
│                                     │
│         [컴퓨터 선택 표시]            │  ← 큰 아이콘/이모지
│              🤖 ?                   │
│                                     │
├─────────────────────────────────────┤
│                                     │
│           승리! / 패배! / 무승부!     │  ← 결과 (큰 글씨, 색상)
│                                     │
├─────────────────────────────────────┤
│                                     │
│         [나의 선택 표시]              │  ← 큰 아이콘/이모지
│              👤 ?                   │
│                                     │
├─────────────────────────────────────┤
│                                     │
│   ┌─────┐  ┌─────┐  ┌─────┐        │
│   │  ✊  │  │  ✋  │  │  ✌️  │        │  ← 큰 버튼 (터치 영역 넓게)
│   │ 바위 │  │ 보  │  │가위 │        │
│   └─────┘  └─────┘  └─────┘        │
│                                     │
├─────────────────────────────────────┤
│     🏆 3승  💔 2패  🤝 1무            │  ← 전적 표시
│                                     │
│         [ 기록 초기화 ]              │  ← 리셋 버튼
└─────────────────────────────────────┘
```

## UI/UX 설계 원칙 (노년층 대상)

### 시각적 접근성

| 항목 | 규격 |
|:---:|:---:|
| 버튼 크기 | 최소 80x80px (터치 영역 확보) |
| 폰트 크기 | 제목 32px+, 본문 24px+ |
| 색상 대비 | WCAG AA 기준 이상 (4.5:1) |
| 아이콘 | 이모지 사용 (직관적 인식) |

### 색상 팔레트

| 용도 | 색상 | HEX |
|:---:|:---:|:---:|
| 배경 | 밝은 크림 | `#FFF8E7` |
| 주요 버튼 | 진한 파랑 | `#1565C0` |
| 승리 | 진한 초록 | `#2E7D32` |
| 패배 | 진한 빨강 | `#C62828` |
| 무승부 | 진한 노랑/주황 | `#F57C00` |
| 텍스트 | 진한 검정 | `#212121` |

### 인터랙션

- 버튼 호버/클릭 시 명확한 피드백 (크기 변화, 색상 변화)
- 결과 표시 시 간단한 애니메이션 (페이드인)
- 실수 방지를 위한 확인 다이얼로그 (초기화 시)

## 기술 스택

| 항목 | 선택 | 이유 |
|:---:|:---:|:---:|
| 상태 관리 | StatefulWidget | 단순한 로컬 상태, 외부 의존성 불필요 |
| 저장소 | 없음 (세션 내 메모리) | MVP 단순화 |
| 랜덤 | dart:math | 컴퓨터 선택 생성 |

## 파일 구조

```
apps/project_1768556464395/
├── lib/
│   ├── main.dart           # 앱 진입점
│   ├── app.dart            # MaterialApp 설정
│   └── features/
│       └── game/
│           ├── game_screen.dart      # 메인 게임 화면
│           ├── widgets/
│           │   ├── choice_button.dart    # 가위/바위/보 버튼
│           │   ├── result_display.dart   # 결과 표시 위젯
│           │   └── score_board.dart      # 전적 표시 위젯
│           └── models/
│               └── game_state.dart       # 게임 상태 모델
├── web/
│   └── index.html
├── pubspec.yaml
├── analysis_options.yaml
└── PLAN.md
```

## 구현 순서

1. **Flutter 프로젝트 초기화** (5분)
   - `flutter create` 실행
   - `pubspec.yaml` 수정
   - 불필요한 파일 정리

2. **모델 및 상태 정의** (5분)
   - `GameChoice` enum (가위, 바위, 보)
   - `GameResult` enum (승, 패, 무)
   - `GameState` 클래스 (점수, 현재 선택 등)

3. **UI 컴포넌트 구현** (25분)
   - `ChoiceButton` - 큰 터치 버튼
   - `ResultDisplay` - 결과 표시
   - `ScoreBoard` - 전적 표시

4. **게임 로직 구현** (10분)
   - 승패 판정 로직
   - 랜덤 컴퓨터 선택
   - 점수 업데이트

5. **메인 화면 조립** (10분)
   - 레이아웃 구성
   - 상태 연결
   - 초기화 기능

6. **테스트 및 마무리** (5분)
   - 웹 실행 테스트
   - UI 미세 조정

## 게임 로직

### 승패 판정 규칙

```
가위 > 보 (가위가 보를 자름)
바위 > 가위 (바위가 가위를 부숨)
보 > 바위 (보가 바위를 감쌈)
같은 선택 = 무승부
```

### 코드 로직 (의사 코드)

```dart
GameResult judge(GameChoice player, GameChoice computer) {
  if (player == computer) return GameResult.draw;

  if ((player == scissors && computer == paper) ||
      (player == rock && computer == scissors) ||
      (player == paper && computer == rock)) {
    return GameResult.win;
  }

  return GameResult.lose;
}
```

## 확장 가능성 (MVP 이후)

- SharedPreferences를 사용한 전적 영구 저장
- 사운드 효과 추가
- 다국어 지원
- 난이도 조절 (컴퓨터 AI 패턴)
- 연승 보너스 표시

## 의존성

```yaml
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
```

외부 패키지 없이 Flutter SDK만으로 구현 가능합니다.
